import type { ToolMeta } from '../registry'

const meta: ToolMeta = {
  slug: 'http-status-codes',
  name: 'HTTP Status Code Reference',
  description: 'Search HTTP status codes by number, name, or explanation.',
  tags: ['http', 'status', 'codes', 'api', 'reference'],
  category: 'dev-utils',
}

export default meta
