import type { ToolMeta } from '../registry'

const meta: ToolMeta = {
  slug: 'uuid-generator',
  name: 'UUID Generator & Validator',
  description: 'Generate version 4 UUIDs in bulk and validate any UUID format and version.',
  tags: ['uuid', 'guid', 'v4', 'generator', 'validator', 'random'],
  category: 'dev-utils',
}

export default meta
